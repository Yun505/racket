#lang racket
(define x 5)
(define (y) 5)
(define (far->cel n)
 (* (/ 5.0 9.0) (- n 32.0)))

(define (far->cel_int n)
 (* (/ 5 9) (- n 32)))

;(define (f x) (+ x 3))
(sqrt pi) ; semi colon comments, square root of pi :D

#;(define
  (f x)
  (+3
   (* 2 x))); hashtag semicolon comments an entire paren block

#| this is a comment block
Horray
uaskldfj;sdf
|#

